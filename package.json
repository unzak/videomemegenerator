{
  "name": "videomemegenerator",
  "version": "0.1.0",
  "description": "Generador de video memes de Cabronazi: la plantilla del PSD sobre un video vertical, montado en el propio navegador",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit"
  },
  "engines": { "node": ">=20" },
  "license": "MIT",
  "dependencies": {
    "@ffmpeg/ffmpeg": "^0.12.15",
    "@ffmpeg/util": "^0.12.2"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "vite": "^6.0.0"
  }
}
